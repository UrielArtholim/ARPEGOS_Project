<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Visual="Material"
             x:Class="ARPEGOS.Views.SidebarView">
    <ContentView.Content >
        <StackLayout Padding="10,0,10,10" BackgroundColor="Aqua"
                   Orientation="Horizontal" 
                   HorizontalOptions="FillAndExpand">
            <Label Text="{Binding SidebarName}"/>
            <ProgressBar Progress="{Binding SidebarValue}" HorizontalOptions="FillAndExpand"
                         ProgressColor="Red"/>
            <Label Text="{Binding SidebarValue}" />
        </StackLayout>
    </ContentView.Content>
</ContentView>