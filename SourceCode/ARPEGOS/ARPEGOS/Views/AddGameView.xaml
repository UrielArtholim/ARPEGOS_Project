<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ARPEGOS.Views.AddGameView">
    <ContentPage.Content>
        <StackLayout Spacing="5" Padding="0,20,0,0" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
            <Entry Text ="{Binding Game}" Placeholder="Enter game name here" HorizontalOptions="CenterAndExpand" />
            <Button Text="Check game" Command="{Binding CheckCommand}" HorizontalOptions="CenterAndExpand"/>
            <Label Text="{Binding CheckMessage}" HorizontalOptions="CenterAndExpand" />
            <Button Text="Select game file" Command="{Binding SelectCommand}" HorizontalOptions="CenterAndExpand"/>
            <Label Text="{Binding SelectMessage}" HorizontalOptions="CenterAndExpand" />
            <Button Text="Add game file" Command="{Binding AddCommand}" HorizontalOptions="CenterAndExpand"/>
            <Label Text="{Binding AddMessage}" HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>