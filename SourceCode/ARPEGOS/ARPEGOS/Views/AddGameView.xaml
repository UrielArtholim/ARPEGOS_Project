<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Visual="Material"
             Title="Añadir juego"
             x:Class="ARPEGOS.Views.AddGameView">
    <ContentPage.Content>
        <StackLayout Spacing="10" Padding="20,20,20,20" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
            <Entry Text ="{Binding Game}" Placeholder="Enter game name" HorizontalOptions="FillAndExpand" />
            <Button Text="Check game" Command="{Binding CheckCommand}" HorizontalOptions="CenterAndExpand"/>
            <Label Text="{Binding CheckMessage}" HorizontalOptions="CenterAndExpand" />
            <Button Text="Select file" Command="{Binding SelectCommand}" HorizontalOptions="CenterAndExpand"/>
            <Label Text="{Binding SelectMessage}" HorizontalOptions="CenterAndExpand" />
            <Button Text="Add file" Command="{Binding AddCommand}" HorizontalOptions="CenterAndExpand"/>
            <Label Text="{Binding AddMessage}" HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>